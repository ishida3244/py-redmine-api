---
name: py-works
services:
  ide:
    build:
      context: .
      args:
        LOCAL_UID: ${LOCAL_UID:-1000}
        LOCAL_GID: ${LOCAL_GID:-1000}
        VERSION: "3.13.13-slim-trixie"
    image: py-works
    environment:
      TZ: Asia/Tokyo
    init: true
    logging:
      driver: json-file
      options:
        max-size: 3m
        max-file: 1
    network_mode: host
    tty: true
    user: vscode
    volumes:
      - ~/.ssh:/home/vscode/.ssh
      - ..:/workspace:cached
